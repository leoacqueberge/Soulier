import SwiftUI

enum AppTheme {
    static let background = Color(red: 0.329, green: 0.365, blue: 0.408)       // #545E68
    static let cardBackground = Color.white
    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.55)
    static let tertiaryText = Color(red: 0.55, green: 0.55, blue: 0.58)
    static let ringTrack = Color.white.opacity(0.22)
    static let ringProgress = Color.white
    static let chartBar = Color(red: 0.78, green: 0.78, blue: 0.80)
    static let chartGrid = Color(red: 0.88, green: 0.88, blue: 0.90)
    static let goalLine = Color(red: 0.30, green: 0.85, blue: 0.39)
    static let averageLine = Color(red: 0.35, green: 0.65, blue: 0.98)
    static let currentIndicator = Color(red: 0.95, green: 0.30, blue: 0.28)
    static let todayHighlight = Color(red: 0.95, green: 0.30, blue: 0.28)
    static let cardTitle = Color.black
    static let cardSecondary = Color(red: 0.45, green: 0.45, blue: 0.47)
    static let segmentBackground = Color(red: 0.92, green: 0.92, blue: 0.94)
}
